1. "config.txt" contains 21 gamma parameters.
2. "inputs.txt" in the order of rows
	a. Budget to install generators ($).
	b. Minimum electricity generation capacity to be installed (MW).
	c. Annualized capital cost of installing a unit of generator type j ($ per MW).
	d. Cost of producing a unit of energy per hour of generator type j ($ per MW per hour).
	e. Duration of period i (hour).
	f. Unit subcontarcting cost for load during period i ($ per MW).
	g. Probability of having load during period i in point k.
	h. Load during period i in point k.

